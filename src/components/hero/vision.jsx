import Button from '../button';

const VisionSection = () => {
	return (
		<div className="vision-section">
			<div className="vision-intro">Introduction</div>
			<h4 className="vision-title">
				A vision for liveable, sustainable & affordable.
			</h4>
			<Button className="vision-btn">Start Exploring</Button>
		</div>
	);
};

export default VisionSection;
